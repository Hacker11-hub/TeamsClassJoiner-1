<Window x:Class="TeamsClassJoiner.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Teams Class Joiner"
        Height="650"
        Width="1050"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->

        <StackPanel Grid.Row="0">

            <TextBlock Text="Teams Class Joiner"
                       FontSize="28"
                       FontWeight="Bold"/>

            <TextBlock Text="Automatically open your Microsoft Teams classes"
                       FontSize="14"
                       Margin="0,5,0,20"/>

        </StackPanel>


        <!-- Controls -->

        <Grid Grid.Row="1"
              Margin="0,0,0,15">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       VerticalAlignment="Center"
                       FontSize="15"
                       FontWeight="SemiBold"/>

            <Button Grid.Column="1"
                    Content="Add Class"
                    Width="110"
                    Height="35"
                    Margin="5"
                    Click="AddClass_Click"/>

            <Button Grid.Column="2"
                    Content="Edit"
                    Width="90"
                    Height="35"
                    Margin="5"
                    Click="EditClass_Click"/>

            <Button Grid.Column="3"
                    Content="Delete"
                    Width="90"
                    Height="35"
                    Margin="5"
                    Click="DeleteClass_Click"/>

        </Grid>


        <!-- Schedule -->

        <DataGrid x:Name="ScheduleGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Class"
                                    Binding="{Binding ClassName}"
                                    Width="180"/>

                <DataGridTextColumn Header="Day"
                                    Binding="{Binding Day}"
                                    Width="120"/>

                <DataGridTextColumn Header="Start"
                                    Binding="{Binding StartTime}"
                                    Width="100"/>

                <DataGridTextColumn Header="End"
                                    Binding="{Binding EndTime}"
                                    Width="100"/>

                <DataGridTextColumn Header="Teams Meeting"
                                    Binding="{Binding TeamsUrl}"
                                    Width="*"/>

                <DataGridCheckBoxColumn Header="Enabled"
                                        Binding="{Binding Enabled}"
                                        Width="80"/>

            </DataGrid.Columns>

        </DataGrid>


        <!-- Footer -->

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    Margin="0,15,0,0">

            <CheckBox x:Name="AutoJoinCheckBox"
                      Content="Automatic class joining"
                      VerticalAlignment="Center"
                      Checked="AutoJoinCheckBox_Changed"
                      Unchecked="AutoJoinCheckBox_Changed"/>

            <StackPanel Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="AutoJoin timeout (s):" VerticalAlignment="Center" Margin="0,0,5,0" />
                <TextBox x:Name="AutoJoinTimeoutTextBox" Width="60" VerticalAlignment="Center" />
            </StackPanel>

            <CheckBox x:Name="StartupCheckBox"
                      Content="Start with Windows"
                      Margin="25,0,0,0"
                      VerticalAlignment="Center"
                      Checked="StartupCheckBox_Changed"
                      Unchecked="StartupCheckBox_Changed"/>

            <Button Content="Refresh"
                    Width="90"
                    Height="30"
                    Margin="25,0,0,0"
                    Click="Refresh_Click"/>

        </StackPanel>

    </Grid>

</Window>
    